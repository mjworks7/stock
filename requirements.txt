# Core
anthropic>=0.40.0
pydantic>=2.6
python-dotenv>=1.0
PyYAML>=6.0

# Web dashboard (선택: streamlit run dashboard.py)
streamlit>=1.36

# Market data (free providers)
yfinance>=0.2.40
finance-datareader>=0.9.50
pandas>=2.2
requests>=2.31   # 한국투자증권(KIS) OpenAPI 호출용
# pykrx is optional KR enrichment; install if available on your Python version
pykrx>=1.0.45 ; python_version < "3.13"
